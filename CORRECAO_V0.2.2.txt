FINORA V0.2.2 - CORREÇÃO DO BUILD

O log real do GitHub Actions mostrou:
- erro de sintaxe em lib/screens.dart na função showTransactionDetails;
- erro de sintaxe em lib/screens.dart na função showTransferForm;
- teste padrão test/widget_test.dart procurando uma classe MyApp inexistente.

Nesta correção:
- as duas funções foram reescritas de forma formatada;
- o workflow é novo e remove test/widget_test.dart;
- flutter analyze agora BLOQUEIA o build se houver erro;
- o APK é publicado imediatamente após compilar;
- o AAB é uma etapa separada e não impede o APK.

PARA ATUALIZAR:
1. Copie e substitua os arquivos deste ZIP na raiz do repositório.
2. Confirme que substituiu:
   lib/screens.dart
   .github/workflows/build-android.yml
3. Rode:

git add .
git commit -m "Finora v0.2.2 corrige compilacao"
git push

No Actions, confira se aparecem estas etapas:
- Remover teste padrão do flutter create
- Verificar sintaxe e análise estática
- Compilar APK Release
- Publicar APK

O artifact correto será:
Finora-APK-v0.2.2
