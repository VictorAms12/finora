name: finora
description: Finora - gestão financeira pessoal multiplataforma.
publish_to: "none"
version: 0.5.0+18

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  local_auth: ^3.0.2
  flutter_local_notifications: ^22.2.0
  sqflite: ">=2.4.2+1 <2.4.3"
  sqflite_common_ffi: ">=2.4.0+3 <2.4.1"
  path: ^1.9.1
  flutter_secure_storage: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true